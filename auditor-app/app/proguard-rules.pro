# Keep ATF check classes reachable via reflection-free direct references only;
# no special rules needed since we call them directly. Add rules here if you
# enable minification and hit ClassNotFoundException at runtime.
