{
  "name": "a11y-auditor",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "workspaces": [
    "dashboard",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w dashboard",
    "start": "npm run build && npm start -w server",
    "dev": "npm run dev -w dashboard",
    "test": "npm test -w server && npm test -w dashboard"
  }
}
